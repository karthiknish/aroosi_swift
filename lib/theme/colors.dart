import 'package:flutter/material.dart';

class AppColors {
  // Primary brand (Soft Pink)
  static const Color primary = Color(0xFFEC4899); // 500
  static const Color primaryDark = Color(0xFFBE185D); // 700
  static const Color onPrimary = Colors.white;

  // Secondary brand (Dusty Blue)
  static const Color secondary = Color(0xFF5F92AC); // 500

  // Accent (Muted Gold)
  static const Color accent = Color(0xFFD6B27C); // 500

  // Aurora gradient accents for bespoke UI surfaces
  static const Color auroraRose = Color(0xFFFF8FB7);
  static const Color auroraIris = Color(0xFFA78BFA);
  static const Color auroraSky = Color(0xFF7DD3FC);
  static const Color auroraSunset = Color(0xFFFFB98B);
  static const Color auroraBackground = Color(0xFFFFF6FB);
  static const Color auroraOutline = Color(0x33EC4899);

  // Background & Surface (Clean off-white)
  static const Color background = Color(0xFFFFFFFF); // primary
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceSecondary = Color(0xFFF9F7F5);

  // Text (Muted Charcoal)
  static const Color text = Color(0xFF4A4A4A); // primary
  static const Color muted = Color(0xFF7A7A7A); // secondary

  // Borders
  static const Color borderPrimary = Color(0xFFE5E7EB);

  // Status colors
  static const Color error = Color(0xFFB45E5E);
  static const Color success = Color(0xFF7BA17D);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  static const Color onDark = Colors.white;
}
